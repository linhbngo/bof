#include <stdlib.h>
#include <stdio.h>
#include <string.h>

int bof(char *str) {
    char buffer[BUF_SIZE];
    strcpy(buffer, str);          
    return 0;
}

int main(int argc, char **argv) {
    char str[BADFILE_SIZE];
    FILE *badfile;

    badfile = fopen("badfile", "r");
    fread(str, sizeof(char), BADFILE_SIZE, badfile);
    bof(str);
    printf("Returned Properly\n");
    return 0;
}
